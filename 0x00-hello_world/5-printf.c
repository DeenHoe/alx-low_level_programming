#include <stdio.h>

/**
 * main - It is the first function of a C program
 *
 * Return: is always 0
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
