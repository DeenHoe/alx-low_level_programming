#include "main.h"
/**
 * mul - multiplies the two integers
 * @a: first parameter
 * @b: second parameter
 * Return: returns the result of the multiplication
 */

int mul(int a, int b)
{
	int c = a * b;

	return (c);
}
