#include <stdio.h>
/**
 * main - prints the name of the program
 * @argc: unsued parameter
 * @argv: pointer to a string
 * Return: 0
 */

int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n", argv[0]);
	return (0);
}
