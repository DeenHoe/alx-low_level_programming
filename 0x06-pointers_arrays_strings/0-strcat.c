#include "main.h"
#include <string.h>
/**
 * _strcat - function to concatenate a string into another
 * @dest: string to be appended unto
 * @src: string to be appended
 * Return: dest
 */

char *_strcat(char *dest, char *src)
{
	char *ade = strcat(dest, src);

	return (ade);
}
