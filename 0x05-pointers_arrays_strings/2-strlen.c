#include "main.h"
/**
 * _strlen - prints the string length
 * @s: function parameter
 * Return: returns a, the value of strlen(str)
 */

int _strlen(char *s)
{
	int str;
	s = &str;

	int a = strlen(str);

	return (a);
}
